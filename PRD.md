# 打卡记录应用 — 产品需求文档（PRD）

**文档版本**：1.0  
**最后更新**：2026-03-31  
**状态**：草案

---

## 1. 背景与目标

### 1.1 背景

用户需要一个轻量的「打卡记录」工具，通过单次点击完成打卡，并保留带准确时间戳的历史记录，界面风格偏海外主流炫酷站点，且支持明暗主题切换。

### 1.2 产品目标

- 提供**一键打卡**能力，降低使用门槛。
- 每条记录包含**可信赖的当前时间**，便于追溯。
- 视觉与交互上达到**现代、炫酷、专业**的观感，默认偏**黑灰暗色系**。
- 通过**主题 Provider** 统一管理主题色，并支持**暗系 / 亮系**切换。

### 1.3 非目标（本期不做）

- 用户账号、多端同步、云端存储（除非后续 PRD 扩展）。
- 复杂的统计报表、导出、提醒通知等（未在需求中提出则默认不包含）。

---

## 2. 用户与场景

### 2.1 目标用户

需要快速记录「某时刻已到场 / 已完成动作」的个人用户（如习惯打卡、到岗记录、简单工时锚点等）。

### 2.2 核心场景

| 场景 | 描述 | 期望结果 |
|------|------|----------|
| 打卡 | 用户打开页面，点击大按钮「打卡」 | 立即生成一条记录，时间为点击瞬间的准确本地时间（或产品约定的时区策略） |
| 浏览记录 | 用户查看历史 | 能按时间理解先后顺序（列表或等价展示，具体 UI 由设计在实现阶段细化） |
| 换主题 | 用户切换明暗 | 全局样式随 Provider 配置切换，无闪烁或明显布局错乱 |

---

## 3. 功能需求

### 3.1 打卡记录模块（P0）

**FR-3.1.1 主操作**  
- 页面主体为**大尺寸「打卡」按钮**，为主要视觉与操作焦点。  
- 用户点击后**立即**创建一条打卡记录。

**FR-3.1.2 记录内容**  
- 每条记录至少包含：**准确打卡时间**（精度建议到秒；格式在实现中统一，如 `YYYY-MM-DD HH:mm:ss` 或 ISO 8601）。  
- 时间来源：以用户执行点击时的**当前时间**为准（浏览器 `Date` 或等价 API）。

**FR-3.1.3 记录展示**  
- 需能展示已生成的记录列表（或等价 UI），使用户可确认打卡已成功且可回看历史。  
- 新记录与旧记录的排序规则：**默认按时间倒序**（最新在上）为推荐默认，若与视觉稿冲突以实现阶段为准并在 PRD 附录注明。

**FR-3.1.4 数据持久化**  
- 需求文档未强制云端；**建议**至少使用 `localStorage` 或同等方案实现刷新后记录仍存在。若产品坚持「仅会话内有效」，需在评审中明确；本 PRD **默认推荐本地持久化**。

### 3.2 主题与视觉（P0）

**FR-3.2.1 默认风格**  
- 整体为**黑灰色系**为主，参考海外主流炫酷站点（高对比、适度渐变/光效、清晰层级），具体由设计与 Tailwind 令牌落地。

**FR-3.2.2 主题 Provider**  
- 使用 **React Context（或等价 Provider）** 集中管理：  
  - 当前主题模式：**暗系（dark）** / **亮系（light）**  
  - 主题色与语义色变量（主色、背景、表面、文字主/次、边框等）  
- 切换主题后，**全应用**相关组件应同步更新（含打卡按钮、列表、布局背景等）。

**FR-3.2.3 动效**  
- 使用 **Framer Motion** 为关键交互提供适度动效（如按钮按压、列表项出现），需保证性能与可访问性（尊重 `prefers-reduced-motion` 为建议项）。

### 3.3 工程与资产（P0）

**FR-3.3.1 技术栈**  
- **React** + **TypeScript** + **Tailwind CSS** + **Vite** + **Framer Motion**。

**FR-3.3.2 图标**  
- 使用 **Font Awesome** 作为图标库（版本与引入方式在技术方案中确定：如 `@fortawesome/react-fontawesome` + SVG 子集）。

**FR-3.3.3 版本管理**  
- 新增 **`.gitignore`**，包含 Node/Vite/React 常见忽略项（如 `node_modules`、`dist`、环境文件、编辑器目录等）。

---

## 4. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 首屏可交互时间合理；打卡操作无刻意延迟；列表在百条量级内滚动流畅。 |
| 兼容 | 支持现代浏览器最近两个大版本（Chrome / Safari / Firefox / Edge 等，以团队标准为准）。 |
| 可维护性 | TypeScript 严格模式建议开启；主题与颜色尽量走设计令牌，避免魔法色散落。 |
| 可访问性 | 按钮具备可聚焦与可读标签；主题切换控件可被键盘操作（建议）。 |

---

## 5. 信息架构与页面结构（建议）

```
应用根布局
├── 主题 Provider（包裹全局）
├── 顶栏/角标区：主题切换（暗/亮）
└── 主内容区
    ├── 打卡记录模块
    │   ├── 大按钮「打卡」
    │   └── 记录列表（时间 + 可选序号/图标）
    └── （本期无其他模块）
```

---

## 6. 验收标准（摘要）

1. 使用指定技术栈可成功构建与运行。  
2. 点击「打卡」后，列表中出现新条目，时间与会话内真实点击时间一致（误差在 1 秒内可接受）。  
3. 切换暗/亮主题后，整体配色与对比度符合「暗系默认炫酷、亮系可读」的预期，且由同一 Provider 驱动。  
4. 存在 `.gitignore` 且忽略项符合常规前端项目。  
5. 界面中如需图标，来自 Font Awesome。  

---

## 7. 风险与待决事项

| 项目 | 说明 |
|------|------|
| 时区 | 默认本地时间是否足够；若需「固定时区」需在 v1.1 明确。 |
| 数据量 | 仅本地存储时，大量记录是否需要分页或归档策略。 |
| 设计稿 | 若无独立设计稿，以 PRD + Tailwind 主题令牌由前端实现「炫酷」风格，需产品一次视觉验收。 |

---

## 8. 附录：需求来源对照

| 原始需求条目 | 对应章节 |
|--------------|----------|
| 技术栈 React + TS + Tailwind + Vite + Framer Motion | §3.3.1、§4 |
| 打卡模块与大按钮、准确时间 | §3.1 |
| 黑灰主题、Provider、明暗切换 | §3.2 |
| `.gitignore` | §3.3.3、§6 |
| Font Awesome | §3.3.2、§6 |

---

*本文档由 `requirenment.txt` 整理扩展而成，后续迭代请更新版本号与变更说明。*
